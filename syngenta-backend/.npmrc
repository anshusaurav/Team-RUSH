# Skip native addon compilation for all packages.
# brain.js pulls in `gl` (OpenGL/WebGL bindings) but we import
# brain.js/dist/browser.js at runtime which is pure JS — no GL needed.
# Render's build environment has no X11 headers so `gl` fails to compile.
ignore-scripts=true
